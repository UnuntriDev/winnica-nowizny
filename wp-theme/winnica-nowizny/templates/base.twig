<!DOCTYPE html>
<html class="no-js" {{ function('language_attributes') | raw }}>
<head>
  <meta charset="{{ site.charset | e('html_attr') }}">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script>document.documentElement.classList.remove('no-js');document.documentElement.classList.add('js');</script>
  {{ function('wp_head') | raw }}
</head>
<body class="{{ body_class | e('html_attr') }}">
  {{ function('wp_body_open') | raw }}

  <a class="skip-link" href="#main">Przejdź do treści</a>

  {% include 'partials/nav.twig' %}

  {# tabindex="-1" so the skip link actually moves focus here, not just the scroll position. #}
  <main id="main" tabindex="-1">
    {% block content %}{% endblock %}
  </main>

  {% include 'partials/footer.twig' %}

  {% include 'partials/consent-banner.twig' %}

  {{ function('wp_footer') | raw }}
</body>
</html>
