{% extends "base.twig" %}

{% block content %}
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-overlay"></div>
  <div class="hero-content">
    <div class="hero-line"></div>
    <p class="hero-label">Błąd 404</p>
    <h1>Strona nie istnieje</h1>
    <p class="hero-subtitle">Może szukasz naszych win lub chcesz zaplanować wizytę?</p>
    <div class="hero-buttons">
      <a href="{{ function('esc_url', function('home_url', '/')) }}" class="btn-primary">Strona główna</a>
      <a href="{{ function('esc_url', function('home_url', '/#wina')) }}" class="btn-outline">Nasze wina</a>
    </div>
  </div>
</section>
{% endblock %}
